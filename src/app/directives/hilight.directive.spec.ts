import { HilightDirective } from './hilight.directive';

describe('HilightDirective', () => {
  it('should create an instance', () => {
    const directive = new HilightDirective();
    expect(directive).toBeTruthy();
  });
});
